<div class="max-w-md mx-auto">
  <h2 class="text-2xl font-bold mb-6 text-center">새 비밀번호 설정</h2>
  
  <% if flash[:alert] %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
      <%= flash[:alert] %>
    </div>
  <% end %>
  
  <p class="text-sm text-gray-600 mb-4">
    새로 사용할 비밀번호를 입력해주세요.
  </p>
  
  <%= form_with url: password_reset_path, method: :patch, local: true, class: "space-y-4" do |f| %>
    <%= hidden_field_tag :token, params[:token] %>
    <div>
      <%= f.label :password, "새 비밀번호", class: "block text-sm font-medium mb-1" %>
      <%= f.password_field :password, required: true, minlength: 6, autofocus: true, class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" %>
      <p class="text-xs text-gray-500 mt-1">최소 6자 이상</p>
    </div>
    
    <div>
      <%= f.label :password_confirmation, "새 비밀번호 확인", class: "block text-sm font-medium mb-1" %>
      <%= f.password_field :password_confirmation, required: true, minlength: 6, class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" %>
    </div>
    
    <div>
      <%= f.submit "비밀번호 변경", class: "w-full bg-blue-600 text-white py-3 px-4 rounded-lg font-medium hover:bg-blue-700 cursor-pointer" %>
    </div>
  <% end %>
  
  <div class="mt-6">
    <%= link_to "← 처음부터 다시하기", password_reset_path, class: "text-gray-600 text-sm inline-flex items-center hover:text-gray-800" %>
  </div>
</div>