<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Mone Music Practice" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#000000">
    <meta name="turbo-cache-control" content="no-cache">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50" data-turbo="<%= controller_name.start_with?('devise') || controller_name.start_with?('sessions') || controller_name.start_with?('registrations') ? 'false' : 'true' %>">
    <% if user_signed_in? %>
      <nav class="bg-white shadow-sm border-b">
        <div class="px-4 py-3">
          <div class="flex justify-between items-center">
            <h1 class="text-lg font-bold">모네뮤직 연습실 시스템</h1>
            <div class="flex items-center gap-4">
              <span class="text-sm"><%= current_user.name %></span>
              <%= button_to "로그아웃", destroy_user_session_path, 
                  method: :delete,
                  form: { data: { turbo: false } },
                  class: "text-sm text-red-600 bg-transparent border-0 p-0 underline cursor-pointer hover:text-red-800" %>
            </div>
          </div>
          <% if current_user.is_admin? %>
            <div class="mt-2 pt-2 border-t">
              <%= link_to "관리자", admin_root_path, class: "text-sm text-blue-600" %>
            </div>
          <% end %>
        </div>
      </nav>
    <% end %>
    
    <main class="px-4 py-4">
      <%= yield %>
    </main>
  </body>
</html>